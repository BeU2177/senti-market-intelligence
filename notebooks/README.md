# Senti Market Intelligence - Notebooks

This directory contains exploratory research, prototyping, and analysis notebooks.

## Usage Guidelines
1. Do not place production credentials or secrets in notebooks.
2. Production code logic (data ingestion, validation, service methods) should be imported from the core package (`config`, `data`, `services`) rather than re-implemented in notebooks.
3. Clean and clear notebook outputs prior to committing.
